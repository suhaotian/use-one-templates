/** Generator by `use-one-templates` https://github.com/suhaotian/use-one-templates */
import type { {{pascalCase name}}Store, State } from './index';

export function getSelectors(store: {{pascalCase name}}Store, initialState: State)  {
  return {
    initialState,
    get state() {
      return store.getState();
    },
  }
}