/** Generator by `use-one-templates` https://github.com/suhaotian/use-one-templates */

import { create, persistStore, isClient } from 'use-one';

const initialState = {
  ready: false, // required
};
const [use, store] = create<typeof initialState>(initialState);
isClient && persistStore(store, {key: "@__{{name}}"});

const selectors = {
  initialState,
  get state()  {
    return store.getState(); 
  },
};

const actions = {
  use,
  reset() {
    store.setState(initialState);
  },
};

export const {{camelCase name}}Store = Object.assign(actions, selectors, store);
